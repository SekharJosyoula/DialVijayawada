<UserControl x:Class="DV.TeleCallerHelper.Admin.Views.BusinessUnitAddEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <!--TODO : Add user controls for businhess unit fields here-->
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Button Content="Save" Command="{Binding SaveBusinessUnitCommand}" />
            <Button Content="Reset" Command="{Binding ResetBusinessUnitCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
